#!/usr/bin/env bash
# -*- wisp -*-
exec -a "$0" guile --language=wisp -x .w -L "$(dirname "$(realpath "$0")")" -C "$(dirname "$(realpath "$0")")" -e '(run-guiletally)' -c '' "$@"
; !#
define-module : run-guiletally
  . #:export : main

import : guiletally hello

define : main args
  hello-world

